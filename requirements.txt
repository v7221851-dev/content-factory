# Streamlit Cloud — минимальный набор без нативных расширений (uvloop и т.п.)
streamlit==1.41.1
httpx==0.27.0
feedparser==6.0.11
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0
pydantic==2.10.3
pydantic-settings==2.6.1
apscheduler==3.10.4
loguru==0.7.3

# API (embedded-режим их не запускает, но импорты есть)
fastapi==0.115.14
uvicorn==0.32.1
python-multipart==0.0.17

# PostgreSQL (Neon/Supabase) — раскомментируйте при DATABASE_URL=postgresql+asyncpg://...
# asyncpg==0.30.0
